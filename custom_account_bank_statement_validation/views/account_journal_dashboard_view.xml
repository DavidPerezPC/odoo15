<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <record id="custom_account_bank_statement_kanaban_view" model="ir.ui.view">
            <field name="name">custom.account.bank.statement.view</field>
            <field name="model">account.journal</field>
            <field name="inherit_id" ref="account.account_journal_dashboard_kanban_view"/>
            <field name="arch" type="xml">
                <xpath expr="//div[@name='bank_cash_commands']" position="before">
                    <t t-if="journal_type == 'bank'">
                        <div name="bank_cash_commands">
                            <t t-if="journal_type == 'bank'">
                                <a role="menuitem" type="object" name="create_bank_statement_from_import_banks_csv">Importe y validación de extracto</a>
                            </t>
                        </div>
                    </t>
                </xpath>
            </field>
        </record>
    </data>
</odoo>