<odoo>
    <data>
        <!-- explicit list view definition -->
        <!--
            <record model="ir.ui.view" id="promotions_active.list">
              <field name="name">promotions_active list</field>
              <field name="model">promotions_active.promotions_active</field>
              <field name="arch" type="xml">
                <tree>
                  <field name="name"/>
                  <field name="value"/>
                  <field name="value2"/>
                </tree>
              </field>
            </record>
        -->

        <!-- actions opening views on models -->
        <!--
            <record model="ir.actions.act_window" id="promotions_active.action_window">
              <field name="name">promotions_active window</field>
              <field name="res_model">promotions_active.promotions_active</field>
              <field name="view_mode">tree,form</field>
            </record>
        -->

        <!-- server action to the one above -->
        <!--
            <record model="ir.actions.server" id="promotions_active.action_server">
              <field name="name">promotions_active server</field>
              <field name="model_id" ref="model_promotions_active_promotions_active"/>
              <field name="state">code</field>
              <field name="code">
                action = {
                  "type": "ir.actions.act_window",
                  "view_mode": "tree,form",
                  "res_model": model._name,
                }
              </field>
            </record>
        -->

        <!-- Top menu item -->
        <!--
            <menuitem name="promotions_active" id="promotions_active.menu_root"/>
        -->
        <!-- menu categories -->
        <!--
            <menuitem name="Menu 1" id="promotions_active.menu_1" parent="promotions_active.menu_root"/>
            <menuitem name="Menu 2" id="promotions_active.menu_2" parent="promotions_active.menu_root"/>
        -->
        <!-- actions -->
        <!--
            <menuitem name="List" id="promotions_active.menu_1_list" parent="promotions_active.menu_1"
                      action="promotions_active.action_window"/>
            <menuitem name="Server to list" id="promotions_active" parent="promotions_active.menu_2"
                      action="promotions_active.action_server"/>
        -->
        <!-- Inherit Form View to Modify it -->
        <record id="active_promotions" model="ir.ui.view">
            <field name="name">active.promotions</field>
            <field name="model">coupon.program</field>
            <field name="inherit_id" ref="coupon.coupon_program_view_tree"/>
            <field name="arch" type="xml">

                <xpath expr="//field[@name='active']" position="after">
                    <!-- Add your fields or attributes here -->
                    <field name="x_as" optional="hide"/>
                    <field name="partners" optional="hide"/>
                    <field name="products" optional="hide"/>
                    <field name="route" optional="hide"/>
                </xpath>



            </field>
        </record>
    </data>
</odoo>